<div class="page-header">
  <h2>All Categories</h2>
  <a href="/categories/create" class="btn">+ Add Category</a>
</div>

<div class="grid">
  <% categories.forEach(category => { %>
    <div class="card">
      <h3><%= category.name %></h3>
      <a href="/categories/category/<%= category.id %>" class="link">
        View →
      </a>
    </div>
  <% }) %>
</div>
